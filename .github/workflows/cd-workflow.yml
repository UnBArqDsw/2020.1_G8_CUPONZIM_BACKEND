name: Deploy

on:
  push:
    branches:
      - master
              
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.5.7 # This is the action
        with:
          heroku_api_key: e3d30a91-fc6c-4a65-861d-408167aa97cb
          heroku_app_name: "cuponzim" #Must be unique in Heroku
          heroku_email: "jlfz06@gmail.com"
          usedocker: true